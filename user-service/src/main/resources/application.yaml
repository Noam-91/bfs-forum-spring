spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/bfsforum
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: H7z0j41996

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  cloud:
    stream:
      function:
        definition: emailVerificationReplyConsumer
      bindings:
        tokenVerify-out-0:
          destination: token-verify-request
        emailVerificationReplyConsumer-in-0:
          destination: token-verify-reply

  kafka:
    bootstrap-servers: localhost:9092

eureka:
  client:
    enabled: false

bfs-forum:
  kafka:
    user-register-binding-name: tokenVerify-out-0
    token-verify-binding-name: tokenVerify-out-0